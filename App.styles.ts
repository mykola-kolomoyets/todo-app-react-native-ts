import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  wrapper: {
    padding: 24,
    marginTop: 32
  },
  goalsContainer: {
    marginTop: 24,
    flexDirection: 'column'
  }
});